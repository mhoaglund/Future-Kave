using UnityEngine;
using System.Collections;

public class CheckIDs : MonoBehaviour
{
		//This is a utility for eliminating duplicate trigger events so we don't have ambiguous nodes
		//spawned in the same place.
		// Use this for initialization
		void Start ()
		{
	
		}
	
		// Update is called once per frame
		void Update ()
		{
	
		}
}

